% Fig. 5 — vendor deployment application and the laboratory map.
% Used by sections/06-evaluation. Screen capture taken over ADB, cropped by
% tools/prepare_assets.py; see that file for the capture command.
% Checked for identifying marks: none present.
\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{vendor_map}
\caption{The vendor deployment application, showing the \SI{1235}{\meter\squared} laboratory map
used throughout this study and the eleven annotations the chassis marker manager holds for it.
The status bar reports the localization confidence that gates every navigation command. Reusing
these annotations avoids the drift that affects coordinates extracted offline, but it does not
make navigation more reliable than reconstructed coordinates (Section~\ref{sec:eval}-C).}
\label{fig:vendormap}
\end{figure}
