% Fig. 5 — vendor deployment application and the laboratory map.
% Used by sections/06-evaluation. Source image: assets/vendor_map.jpg,
% regenerated by tools/prepare_assets.py.
% Checked for identifying marks: none present.
%
% TO REPLACE with a clean screenshot: capture the deployment application at
% full screen resolution rather than photographing the tablet, drop the file
% in assets/ under the same name, and delete the crop/resize call for
% vendor_map in tools/prepare_assets.py so the screenshot is used as-is.
\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{vendor_map}
\caption{The vendor deployment application showing the \SI{1235}{\meter\squared} laboratory map
used throughout this study, with its annotated points of interest. These annotations live in the
chassis marker manager; reusing them avoids the drift that affects coordinates extracted
offline, but does not by itself make navigation succeed (Section~\ref{sec:eval}-C).}
\label{fig:vendormap}
\end{figure}
