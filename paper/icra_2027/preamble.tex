% ============================================================================
%  Preamble — packages, macros, global settings.
%  Edit here for anything that affects the whole document.
% ============================================================================

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{cite}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}   % >{\raggedright\arraybackslash}p{} in narrow table columns
\usepackage{url}
\usepackage{siunitx}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta,positioning,fit,calc,decorations.pathreplacing}
\usepackage[section]{placeins}
\usepackage{stfloats}
\usepackage{float}

% Section VI queues four floats (Fig. 5, Table III, Fig. 6, Table IV) behind
% dense text. LaTeX's defaults (totalnumber=3, floatpagefraction=0.5 with a
% high textfraction) push the fourth onto a page of its own rather than the
% blank space left after the third, which is why a lightly filled page kept
% appearing between Table IV and the discussion. These relax the limit
% without changing how anything looks — floats still land in source order.
\setcounter{totalnumber}{6}
\setcounter{topnumber}{4}
\setcounter{dbltopnumber}{2}
\renewcommand{\textfraction}{0.05}
\renewcommand{\topfraction}{0.95}
\renewcommand{\dbltopfraction}{0.95}
\renewcommand{\floatpagefraction}{0.4}
\renewcommand{\dblfloatpagefraction}{0.4}

% Images live in assets/ ; \includegraphics needs only the base name.
\graphicspath{{assets/}}

% IEEEtran defaults to \flushbottom, which stretches each column's glue to
% reach the full page height. Where a column has almost no natural stretch
% (a short subsection heading followed by a table placed "here" in the next
% column), the class concentrates that stretch into the one flexible gap it
% can find, opening a large blank band under the heading. Ragged bottoms let
% columns end at their natural height instead.
\raggedbottom

% IEEEtran hardcodes \footnotesize inside \thebibliography. With the page
% budget no longer razor-thin, bump the reference list back up to \small
% for readability; the label width IEEEtran precomputes at \footnotesize is
% close enough at \small that entry numbers still line up.
\let\IEEEtranoriginalbibliography\thebibliography
\renewcommand{\thebibliography}[1]{\IEEEtranoriginalbibliography{#1}\small}

\hypersetup{colorlinks=true,linkcolor=black,citecolor=black,urlcolor=black}
\sisetup{detect-all,range-phrase={--},range-units=single}

% ---------------------------------------------------------------------------
%  Terminology macros. Use these instead of typing the words, so that a
%  change of wording stays consistent across every section.
% ---------------------------------------------------------------------------
\newcommand{\ros}{\textsc{ros}}
\newcommand{\rosbridge}{\textsc{rosbridge}}
\newcommand{\rosapi}{\texttt{rosapi}}

% Anonymised designation of the case-study robot. The paper deliberately
% avoids naming the model; change this one macro if that policy changes.
\newcommand{\platform}{the study platform}
\newcommand{\Platform}{The study platform}

% Figures 2 and 3 are monochrome line art and define their own local styles;
% Figure 4 keeps a small amount of colour to separate its three lanes.
% figdark is the neutral ink used by the data plots in Figures 6 and 7.
\definecolor{figdark}{HTML}{2F3A46}
